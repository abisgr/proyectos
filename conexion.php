<?php

    $conexion= mysqli_connect(
        $hostname = "localhost",
        $username= "root",
        $password= "admin",
        $database= "users",
        $port= 8889
    );

    /*
    if($conexion){
        echo "Conexion exitosa";
    }else{echo "Error en conexion";}
    */
    
?>